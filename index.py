import os

nmap <host>
sudo edtable -A FORWAR "196.174.0.105/1


// ssh keygen generator
ssh-keygen -t rsa -b 4096 "correo@correo.com"
// verify if system running ssh key
eval "${ssh-agent -s);
// agrega ckave ssh al siguiente 
ssh-add  ~/.ssh/id_rsa
// key copy
xclip -sel clip < ~/.ssh/id_rsa.pub

